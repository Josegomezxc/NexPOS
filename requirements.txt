# ============================================================
# requirements.txt — Doña Sara (Django POS)
# ============================================================
# Sólo las dependencias top-level que el proyecto usa de forma
# directa. Las transitivas (asgiref, sqlparse, tzdata, packaging)
# las trae pip automáticamente al instalar las de abajo.
# ============================================================

# Framework principal (fijado al major actual para evitar breaks)
Django>=6.1,<6.2

# Driver de PostgreSQL (psycopg2 con binarios precompilados)
psycopg2-binary>=2.9

# Servir archivos estáticos en producción sin nginx/apache
whitenoise>=6.0

# Parsear DATABASE_URL en deploy (Heroku, Render, Railway, etc.)
dj-database-url>=3.1

# Servidor WSGI de producción
gunicorn>=23.0
